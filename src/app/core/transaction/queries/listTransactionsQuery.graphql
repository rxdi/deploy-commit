query listTransactions($status: TransactionsTypeEnum, $repoFolder: String!) {
  listTransactions(status: $status, repoFolder: $repoFolder) {
    _id
    status
    birthtime
    path
    repoFolder
    message
    namespace
  }
}
